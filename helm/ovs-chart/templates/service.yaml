apiVersion: v1
kind: Service
metadata:
  name: {{ include "ovs-chart.fullname" . }}
  labels:
    {{- include "ovs-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ovs-chart.selectorLabels" . | nindent 4 }}
